using Domain.Enums;

namespace Application.Security;

public static class RoleConstants
{
    public const string Admin = nameof(Roles.Admin);
    public const string Player = nameof(Roles.Player);
}
